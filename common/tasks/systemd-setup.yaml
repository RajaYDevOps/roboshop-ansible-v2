- name: "copy {{ component }} service"
  template:
      src: "{{ component }}.service.j2"
      dest: "/etc/systemd/system/{{ component }}.service"

- name: Reload deamon
  systemd_service:
      name: "{{ component }}"
      daemon_reload: true